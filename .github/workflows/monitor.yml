name: Monitor roles

on:
  workflow_dispatch:
  schedule:
    # GitHub may delay or drop scheduled events. Four staggered attempts per
    # hour make it much less likely that a short-lived posting is missed while
    # keeping the monitor inside the requested Eastern-time business window.
    - cron: "7,22,37,52 9-20 * * 1-5"
      timezone: "America/New_York"

permissions:
  contents: write

concurrency:
  group: role-monitor
  cancel-in-progress: false

jobs:
  monitor:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check weekday monitor window
        id: window
        run: |
          day=$(TZ=America/New_York date +%u)
          hour=$(TZ=America/New_York date +%H)
          if [ "${{ github.event_name }}" = "workflow_dispatch" ] || { [ "$day" -le 5 ] && [ "$hour" -ge 9 ] && [ "$hour" -le 20 ]; }; then
            echo "run=true" >> "$GITHUB_OUTPUT"
          else
            echo "run=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Checkout
        if: steps.window.outputs.run == 'true'
        uses: actions/checkout@v4

      - name: Setup Node
        if: steps.window.outputs.run == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Validate monitor parser
        if: steps.window.outputs.run == 'true'
        run: npm run validate

      - name: Run monitor
        if: steps.window.outputs.run == 'true'
        run: npm run monitor
        env:
          FETCH_TIMEOUT_MS: "10000"
          FETCH_RETRIES: "1"
          FETCH_RETRY_BASE_MS: "350"
          ATS_SOURCE_CONCURRENCY: "12"
          SOURCE_SCAN_DEADLINE_MS: "90000"
          DOUBLE_CHECK_TIMEOUT_MS: "20000"
          HTML_DETAIL_CONCURRENCY: "4"
          DISCOVERY_CONCURRENCY: "8"
          DISCOVERY_LIMIT: "500"
          DISCOVERY_REFRESH_HOURS: "168"
          DISCOVERY_ERROR_REFRESH_HOURS: "6"
          SITEMAP_DETAIL_LIMIT: "30"
          DISCOVERY_FEED_CONCURRENCY: "8"
          DISCOVERY_FEED_VERIFY_LIMIT: "2000"
          DISCOVERY_FEED_TIMEOUT_MS: "15000"
          DISCOVERY_FEED_REVERIFY_HOURS: "24"
          MIN_ATS_SUCCESS_PERCENT: "75"
          STALE_AFTER_DAYS: "21"

      - name: Commit generated updates
        if: steps.window.outputs.run == 'true'
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md NEW_GRAD.md INTERNSHIPS.md new-grad internships data/roles.json data/roles.csv data/latest_scan.json data/coverage.json data/source_discovery.json data/company_catalog.json data/notification_outbox.json docs/notifications/catalog.json
          if git diff --cached --quiet; then
            echo "No generated changes to commit."
            exit 0
          fi
          git commit -m "Update 2027 role board"
          for attempt in 1 2 3; do
            git fetch origin main
            if ! git rebase -X theirs origin/main; then
              git rebase --abort
              echo "Unable to rebase generated updates onto the latest main branch."
              exit 1
            fi
            if git push origin HEAD:main; then
              exit 0
            fi
            echo "Push raced with another update; retrying ($attempt/3)."
            sleep $((attempt * 3))
          done
          echo "Unable to push generated updates after 3 attempts."
          exit 1

      - name: Dispatch verified role notifications
        if: steps.window.outputs.run == 'true'
        run: npm run notify
        env:
          NOTIFICATION_API_URL: ${{ secrets.NOTIFICATION_API_URL }}
          NOTIFICATION_INGEST_SECRET: ${{ secrets.NOTIFICATION_INGEST_SECRET }}
